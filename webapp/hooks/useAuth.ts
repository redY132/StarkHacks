/**
 * Re-exports auth context from `contexts/AuthProvider.tsx`.
 * Prefer importing from `@/contexts/AuthProvider` in new code.
 */
export { AuthProvider, AuthRouterShell, useAuth } from '@/contexts/AuthProvider';
